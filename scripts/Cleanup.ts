import { WorldInitializeAfterEvent } from "@minecraft/server";

/** Basic cleanup script to run during world startup to clean up residual entities and such */
export default function WorldCleanup(arg: WorldInitializeAfterEvent) {
  //Logic was moved from here
}